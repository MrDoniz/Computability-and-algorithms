//Va espacio entre simbolos "])" "))" ?
int main (int argc, char *argv[]) {}
if (output_file_program.is_open() && input_file_program.is_open())

//Carpeta lib

//Los colores donde?

//Varios if uno dentro de otro van juntos? sin llaves? preferible los &&?